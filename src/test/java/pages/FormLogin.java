package pages;

import org.openqa.selenium.WebDriver;

public class FormLogin {

    private WebDriver navegador;

    public FormLogin(WebDriver navegador){
        this.navegador = navegador;
    }
}
